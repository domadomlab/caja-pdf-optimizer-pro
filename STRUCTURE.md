# 🏗️ Структура Проекта (Project Structure)

Этот документ описывает архитектуру репозитория **Caja PDF Optimizer Suite**. Мы придерживаемся стандартов разработки пакетов для Debian-based систем.

## 📂 Дерево каталогов

```text
caja-pdf-optimizer-pro/
├── bin/                  # Служебные скрипты для разработчика
│   └── build.sh          # Скрипт автоматической сборки проекта (CI/CD)
├── dist/                 # Артефакты сборки (готовые .deb и .zip файлы)
├── docs/                 # Документация проекта
│   ├── CHANGELOG.md      # История версий и изменений
│   ├── ROADMAP.md        # План развития проекта
│   └── USER_MANUAL.md    # Инструкция для конечного пользователя
├── packaging/            # Файлы, необходимые для создания .deb пакета
│   └── debian/
│       ├── control       # Метаданные пакета (зависимости, версия, описание)
│       └── postinst      # Скрипт, выполняемый после установки (настройка меню)
├── src/                  # Исходный код приложения
│   └── caja-pdf-optimizer # Основной исполняемый Bash-скрипт (ядро логики)
├── LICENSE               # Лицензия проекта (MIT)
├── README.md             # Главная страница проекта (обзор, установка)
└── STRUCTURE.md          # (Этот файл) Описание структуры
```

## 📝 Назначение компонентов

### `src/caja-pdf-optimizer`
Главный исполняемый файл. Содержит логику:
*   Взаимодействия с ImageMagick (`convert`).
*   Логирования операций в `/tmp`.
*   Отправки уведомлений через `notify-send`.
*   Интеллектуальной проверки размера файла.

### `packaging/debian/control`
Паспорт пакета. Определяет:
*   Версию ПО.
*   Зависимости (`imagemagick`, `caja` и т.д.).
*   Описание, видимое в менеджере приложений.

### `packaging/debian/postinst`
Скрипт пост-установки. Критически важный компонент, который:
1.  Очищает систему от старых версий скриптов (конфликтов).
2.  Обновляет кэш десктоп-файлов.
3.  Информирует пользователя об успешной интеграции.
